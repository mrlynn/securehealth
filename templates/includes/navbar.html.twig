<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <i class="fas fa-shield-alt mr-2"></i> SecureHealth
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">Home</a>
                </li>
                
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link" href="/patients">Patients</a>
                    </li>

                    {% if is_granted('ROLE_RECEPTIONIST') and not is_granted('ROLE_DOCTOR') and not is_granted('ROLE_NURSE') and not is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="/scheduling.html">Scheduling</a>
                        </li>
                    {% endif %}

                    {% if is_granted('ROLE_DOCTOR') %}
                        <li class="nav-item">
                            <a class="nav-link" href="/audit-logs">Audit Logs</a>
                        </li>
                    {% endif %}
                {% endif %}
                
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('docs_index') }}">Documentation</a>
                </li>
                
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_help') }}">Help</a>
                </li>
            </ul>
            
            <ul class="navbar-nav">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown">
                            <i class="fas fa-user mr-1"></i> {{ app.user.username }}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right">
                            <a class="dropdown-item" href="/profile">Profile</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>