{
    auto_https off
    admin off
}

:9000 {
    root * public
    
    # FrankenPHP has built-in PHP support
    # Route API requests to Symfony
    rewrite /api/* /index.php
    rewrite /staff/* /index.php
    
    # Handle all other requests
    try_files {path} {path}/ /index.html
    file_server
}
